# SOTN decompilation progress page

Vite + React + TypeScript. Mirrors code coverage from [decomp.dev](https://decomp.dev/Xeeynamo/sotn-decomp).

- `npm install` then `npm run dev` (local) or `npm run build` (outputs `dist/`).
- Deploys to `gh-pages` on push to `docs`.
